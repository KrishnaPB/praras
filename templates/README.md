# Legacy Templates Directory (Deprecated)

> [!NOTE]
> The active canonical single source of truth for global shared templates has moved to:
> **`/components/`**
> 
> *   `components/site-header.html`
> *   `components/site-footer.html`
> *   `components/quote-drawer.html`
> *   `components/search-modal.html`
> 
> Use `python3 tools/sync_components.py` to synchronize components sitewide.
> Historical drafts and unreferenced template files have been archived to `scratch/drafts/legacy_templates/`.
